# JWT Config
jwt.secret=***REMOVED***
jwt.expiration=300

# Server Port
server.port=8080

# App Info
spring.application.name=TFG-ALMACEN

# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/almacen_db_dev
spring.datasource.username=usuario
spring.datasource.password=***REMOVED***

# LOGS
logging.level.org.springframework.security=DEBUG


# JPA / Hibernate
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=always
spring.jpa.show-sql=true


# Hibernate Logging (opcional, para desarrollo)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG

# HikariCP Pool (recomendado para estabilidad, valores ajustables según tu caso)
spring.datasource.hikari.max-lifetime=30000
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.validation-timeout=5000

# (Opcional) Desactiva la advertencia de open-in-view si no lo usas
spring.jpa.open-in-view=false

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=raulfernandez6106@gmail.com
spring.mail.password=***REMOVED***
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.storage.location=uploads

# Configuración para permitir el uso de multipart en las solicitudes
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


